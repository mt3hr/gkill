'use strict'
export enum DropTypeFoldableStruct {
    in_folder_top = 0, // フォルダ内の一番上に移動
    in_folder_bottom = 1, // フォルダ内の一番下煮移動
    up_element = 2, // 要素の一つ上に移動
    down_element = 3 // 要素の一つ下に移動
}
