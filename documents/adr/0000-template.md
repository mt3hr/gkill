# ADR-NNNN: <日本語のタイトル。決定を1行で>

| | |
|---|---|
| Status | Accepted |
| Date | YYYY-MM-DD |
| Sources | `<commit sha>` / `.claude/skills/xxx/SKILL.md`「節名」 |
| Supersedes | なし |
| Superseded-by | なし |
| Anchors | `src/...`（アンカーを置いた場所。無ければ「なし — 理由」） |

## Context

何が問題だったか。どういう制約があったか。この決定が無いとどうなるか。

**規則そのものは書かない。** 規則の正本は `.claude/skills/*/SKILL.md`（横断規約は `AGENTS.md`）とコードコメント。
ここに書くのは「なぜその規則が要るのか」の側だけ。

## Decision

何を決めたか。断定形で1〜3文。

## Rejected alternatives

**この節がこの資料の存在理由。** 採らなかった案と、採らなかった理由。
「AI や新規開発者が言いそうな改善提案」を疑問文の形で先回りして書く。
却下案が1つも無いなら、それは決定ではなく仕様なので `documents/reverse/` の担当。

- **<却下案>** — <なぜ駄目か。できれば実測値つきで>

## Consequences

この決定を守ることで受け入れる制約。守らないと何が起きるか。

**壊れ方が静か（例外もエラーも出ない）なら必ずそう書く。**
テストで守れていない決定は、そのことをここに書く。

## Evidence

実測値・ベンチマーク・プロファイル。

実測が無い決定は空欄にせず、理由を書く。
（例:「実測なし — 脅威モデルからの判断」「実測なし — 構造上の背理」）

## Related tests

- `src/...`（実在するもののみ。verify_docs が実在を検査する）
